<head>
</head>

<body>
  <ul id="links">
    <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url   %>
      Tag:   <%= link.tag   %>
    </li>
  <% end %>
  </ul>
</body>
